<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindu.go.loginjoinjsp.dao.IMemberEmailDao">
    <insert id="insertEmail">
        insert into emailcode (userid, vcode) values (#{userid}, #{vcode});
    </insert>
</mapper>
